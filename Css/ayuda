google fonts(importamos la fuente y seleccionamos import y pegamos en la hoja de estilos)

1 #id, 2 .clase, 3 elemento (orden de prioridad, para css se usa clase, para js id)

color picker para elegir colores

si queremos agarrar todo el ancho 100vw, para larggo se usa vh

background-image:url(...)

color adobe-> paleta de colores

*{
    margin: 0;
    padding: 0;
}
lo hacemos para borrar la config default de las etiquetas

display: flex -> Un cuadrado al lado del otro
flex-direction -> elegis como es muestra
flex-wrap -> se ajustan los cajone de acuerdo al tamaño de la pantalla
flox-flow: combina las de arriba

justified content -> ordena horizontalmente
aling-items -> ordena verticalmente